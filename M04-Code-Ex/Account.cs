//when we are creating other classes you have to do it outside of the main method
//This is a class called Account
//Classes are blueprints with built-in methods (functrions).
//Similar to a structure with added functions..there are other difference we will see in a bit

// //defining a class
class Account{
    //then we have an access modifier type then variable name
    //access modifiers tell us who can have access to the data inside of a class
    //public means anyone internal or external to the class can have access

    public string AccountName;
    public string AccountNumber;
    public double AccountBalance;
}